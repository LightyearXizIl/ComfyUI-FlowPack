# 版本规则

ComfyUI FlowPack 使用三段式版本号 `主版本.次版本.修订版本`，每一位按十进制“满 10 进 1”。

- 修订版本从 0 到 9；`0.0.9` 的下一版是 `0.1.0`。
- 次版本从 0 到 9；`0.9.9` 的下一版是 `1.0.0`。
- 主版本按需要继续递增。
- Git 标签在版本号前加 `v`，例如软件版本 `0.0.1` 对应标签 `v0.0.1`。
