# ComfyUI FlowPack 0.0.1 发布验证

发布日期：2026-09-11  
目标：Windows x64 自包含安装包。

## 本地验证

- 锁定还原：通过。
- Release 自动化测试：12/12 通过。
- App 自包含发布：通过，产品版本 `0.0.1`。
- Worker 自包含发布：通过，直接运行退出码为 0。
- Inno Setup 6.7.3 编译：通过。
- 隔离目录静默安装：通过。
- 安装后真实启动：首页标题和“尚未检查环境”状态可见。
- 静默卸载：通过，主程序文件已移除。
- Authenticode：`NotSigned`，不能声称已签名。
- 本地安装包：`ComfyUI-FlowPack-0.0.1-Setup.exe`，72,460,637 字节。
- 本地安装包 SHA-256：`9B3935FCF0ED5B8FDE85B9E4D86733AF285890A89C4075B55241D6140907CC7B`。

## 功能边界

该安装包验证的是工程壳层、发布内容和安装器生命周期，不是完整业务验收。真实 ComfyUI Desktop 绑定、下载、安装资源、离线包、试运行和维护恢复功能尚未完成，因此 0.0.1 不能用于向真实 ComfyUI 环境部署资源。

## GitHub 发布验证

- 发布提交：`af5893aa247f8db98fbb21c607df5865cf01c390`。
- 标签：`v0.0.1`，已核对指向上述提交。
- GitHub Actions：运行 `34565404527` 成功，Windows job 的还原、测试、发布、安装器构建、工作流产物上传和 Release 创建步骤全部通过。
- GitHub Release：非草稿、非预发布。
- 远程安装包：`ComfyUI-FlowPack-0.0.1-Setup.exe`，72,472,025 字节。
- 远程安装包 SHA-256：`0678AA24C73C8E0CC49E43DF2CA2317AE105114395D8C0D6120ACF27171BA895`。
- GitHub API 返回的服务器资产摘要与远程 `SHA256SUMS.txt` 一致。

本机尝试完整下载远程安装包时未产生有效数据，因此没有登记“远程下载后本机安装”通过。远程证明仅覆盖 CI 成功、正式 Release 状态、标签指向、资产存在，以及 GitHub 服务器摘要与校验清单一致；本地安装生命周期由本机构建的同版本安装包验证。
