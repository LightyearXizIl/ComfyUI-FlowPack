<UserControl x:Class="FlowPack.App.Views.ModelsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             AutomationProperties.AutomationId="Page.Models">
  <Grid>
    <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="16" /><RowDefinition Height="Auto" /><RowDefinition Height="18" /><RowDefinition Height="Auto" /></Grid.RowDefinitions>
    <StackPanel>
      <Button Content="← 返回资源包" Background="Transparent" BorderThickness="0" Foreground="{DynamicResource AccentBrush}" Padding="0" HorizontalAlignment="Left" Cursor="Hand" Command="{Binding NavigateCommand}" CommandParameter="Packages" />
      <TextBlock Text="{Binding PageTitle}" Style="{StaticResource PageTitle}" Margin="0,10,0,0" />
      <TextBlock Text="{Binding PageSubtitle}" Style="{StaticResource SubtleText}" Margin="0,8,0,0" />
    </StackPanel>
    <TextBox Grid.Row="2" Text="{Binding ModelSearchText, UpdateSourceTrigger=PropertyChanged}" ToolTip="按模型、资源包、版本或状态搜索" AutomationProperties.Name="搜索模型" />
    <Grid Grid.Row="4">
      <Border>
        <Border.Style>
          <Style TargetType="Border" BasedOn="{StaticResource EmptyStateCard}">
            <Style.Triggers><DataTrigger Binding="{Binding HasModels}" Value="True"><Setter Property="Visibility" Value="Collapsed" /></DataTrigger></Style.Triggers>
          </Style>
        </Border.Style>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center"><TextBlock Text="尚未导入模型清单" Style="{StaticResource SectionTitle}" /><TextBlock Text="导入包含模型声明的资源包后，这里会列出清单中的模型；实际部署状态仍需环境验证。" Style="{StaticResource SubtleText}" TextAlignment="Center" Margin="0,7,0,0" /></StackPanel>
      </Border>
      <ItemsControl ItemsSource="{Binding ModelView}">
        <ItemsControl.ItemTemplate>
          <DataTemplate>
            <Border Style="{StaticResource Card}" Margin="0,0,0,12">
              <Grid>
                <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="150" /><ColumnDefinition Width="150" /></Grid.ColumnDefinitions>
                <StackPanel>
                  <TextBlock Text="{Binding Name}" FontSize="16" FontWeight="SemiBold" Foreground="{DynamicResource TextBrush}" />
                  <TextBlock Text="{Binding PackageName, StringFormat=来自资源包：{0}}" Style="{StaticResource SubtleText}" Margin="0,5,0,0" />
                  <TextBlock Text="{Binding Status}" Style="{StaticResource SubtleText}" Margin="0,4,0,0" />
                </StackPanel>
                <TextBlock Grid.Column="1" Text="{Binding Size}" Foreground="{DynamicResource TextBrush}" VerticalAlignment="Center" />
                <TextBlock Grid.Column="2" Text="{Binding PackageVersion, StringFormat=v{0}}" Style="{StaticResource SubtleText}" VerticalAlignment="Center" HorizontalAlignment="Right" />
              </Grid>
            </Border>
          </DataTemplate>
        </ItemsControl.ItemTemplate>
      </ItemsControl>
    </Grid>
  </Grid>
</UserControl>
