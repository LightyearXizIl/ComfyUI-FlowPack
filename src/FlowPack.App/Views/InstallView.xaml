<UserControl x:Class="FlowPack.App.Views.InstallView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:views="clr-namespace:FlowPack.App.Views"
             AutomationProperties.AutomationId="Page.Install">
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="20" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>
    <StackPanel>
      <TextBlock Text="{Binding PageTitle}" Style="{StaticResource PageTitle}" />
      <TextBlock Text="{Binding PageSubtitle}" Style="{StaticResource SubtleText}" Margin="0,8,0,0" />
    </StackPanel>
    <TabControl Grid.Row="2" AutomationProperties.Name="安装资源包与预览">
      <TabItem Header="已导入资源包"><views:PackagesView /></TabItem>
      <TabItem Header="预览与依赖"><views:InstallPreviewView /></TabItem>
    </TabControl>
  </Grid>
</UserControl>
