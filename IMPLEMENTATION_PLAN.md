# ComfyUI FlowPack 完整软件执行规划书

文档版本：1.0  
编制日期：2026-09-11  
状态：完整实施基线；M0 已实现，M1–M10 仍待实施，不能据此声称完整软件已经完成。  
当前发布范围：0.0.1 工程预览版，包含 M0 壳层修复、测试与安装器链路，不包含真实 ComfyUI 资源部署能力。

项目官方GitHub仓库：[LightyearXizIl/ComfyUI-FlowPack](https://github.com/LightyearXizIl/ComfyUI-FlowPack)。该地址由用户指定；后续源码协作、GitHub Actions及发布产物以此仓库为目标，不另建同名仓库。

用户已确认：今后源码上传、软件版本更新均使用该仓库；源码推送、版本标签、更新说明、GitHub Releases及安装包统一归属此仓库。此项固定后续交付目的地，具体推送和发布按对应开发/更新任务执行。

仓库关联现状：0.0.1 发布任务开始时远程仓库为空；本地已初始化 `main` 并关联 `origin` 到 https://github.com/LightyearXizIl/ComfyUI-FlowPack.git。`bin/obj`、发布中间目录、本机验收产物、私有配置与凭据均排除在源码提交外；远程推送、标签、Actions 和 Release 结果以 `docs/RELEASE_0.0.1.md` 的后续核验记录为准。

## 1. 交付目标、需求依据与完成定义

软件名称：ComfyUI FlowPack；简称：FlowPack。定位为 Windows x64 官方 ComfyUI Desktop 的工作流资源安装与打包工具。资源包使用者可以导入、检查、安装、验证、维护；创作者可以分析、补全、打包和分享，所有用户都可以创建资源包。

最终交付必须让用户完成以下整套流程：

1. 启动软件 → 选择或重新关联资源库 → 发现并确认目标 Desktop 实例。
2. 导入在线清单、离线包或展开目录 → 得到基于实际环境的安装预览。
3. 确认安装 → 下载、校验、保护性部署 → 取得持久化的分级验证报告。
4. 导入工作流 → 分析并补充依赖 → 四步向导导出在线清单或离线包 → 在另一合格实例重新导入安装。
5. 查看、暂停、恢复、取消和重试任务；重启界面或 Worker 后找回任务及真实结果。
6. 搜索工作流、模型与节点，执行修复、包升级、卸载、清理和资源库迁移。
7. 定制主题、预览、应用、取消、恢复默认、导入导出和手动重新加载。
8. 通过正式自包含安装器安装软件；升级、卸载和重装保留资源库、用户主题及可恢复关联。

### 1.1 需求依据与优先级

- [软件原始需求全文](docs/SOFTWARE_REQUIREMENTS_ORIGINAL.md)：保留用户附件完整内容，作为功能范围的逐项依据。
- [前端视觉规范](FRONTEND_REFERENCE.md)及 frontend-references 下的 9 张 PNG：页面布局、层级、信息密度与默认视觉的依据。
- 根目录 [LOGO 图标.png](LOGO%20图标.png)：唯一品牌图形来源。旧版LOGO目录不参与实现或验收，不要求删除。
- 本对话已确认的合并规则：视觉冲突以视觉规范为准，业务能力按原始实施计划保留；四步向导必须包含原六步的全部工作。
- [需求与验收追踪表](REQUIREMENTS_ACCEPTANCE.md)：每项原需求对应工作包、验收用例和证据。没有通过证据的需求不能标记完成。

### 1.2 “完整”与原有范围限制

下面列出的所有工作包都属于本次产品目标，不把下载、打包、维护或主题持久化移到一个没有交付条件的“以后再说”。

保留原始范围边界：不附带 Desktop、ComfyUI 核心、ComfyUI Python 或显卡驱动；提供官方下载入口。不做商城、账号、云端生成、节点画布、任意布局编辑器、自定义分卷、每工作流独立环境或多设备同时写网络共享库。软件可处理含外部 API 的资源声明，但不提供云端生成服务或自动使用用户未授权的外部账户。

对任意第三方节点、源码编译和安装脚本不承诺通用自动适配。首版必须完成可解析兼容 wheels 与普通节点归档的全流程，并对超出能力范围的内容给出具体阻断原因、人工补充或专门适配入口。未知依赖不等于没有依赖。

软件支持的 Desktop 组合只由真实端到端验证决定；未验证组合允许检查、归档和打包，暂停自动部署。该限制不能用来跳过至少一套真实官方 Desktop 的完整验证。

## 2. 当前现状与必须先纠正的问题

依据本轮源码检查及用户截图，当前版本应归类为“有部分工程骨架、存在阻断缺陷的界面原型”。已有 record、解析辅助类和启动代码可审查后复用，不能沿用旧回复中的完成结论。

| 问题 | 当前证据 | 实施动作 |
|---|---|---|
| 页面全空 | MainWindow.xaml 第26、32、38、44、46、48、50、52、54行的页面根 Grid 都有本地 Visibility=Collapsed，覆盖样式触发器 | 使用单一页面宿主及导航状态；实际加载和点击九页，检查非零内容布局 |
| 导航挤在一起 | MainWindow.xaml 第4行模板未使用设置的 Padding；App.xaml 第6–7行的主次按钮也相同 | 共享模板绑定 Padding、对齐、内容和边框；补足状态与命中区域 |
| 假环境和假进度 | ShellViewModel.cs 第20、26–32、41行写死连接、资源和任务；安装按钮只改百分比 | 正常启动使用真实空态或未检查状态；开发样例必须显式隔离、标注 |
| 页面操作没有业务 | 工作流导入、向导、环境详情、主题导入导出等无命令；搜索与筛选无绑定 | 给每个入口绑定真实服务、错误反馈、忙碌状态和取消能力 |
| 无法访问或选错详情 | 模型/节点缺少导航入口；所有包详情显示固定内容 | 路由携带包/实例/工作流 ID，按选中对象加载与返回 |
| 环境识别不可信 | ComfyUiInspector.cs 第9–11行对任意目录抓第一个 python*.exe 并猜 user 目录 | 重建分版本适配与只读探测，禁止按文件名猜实例 |
| 安装规划无真实依据 | SafeInstallPlanner.cs 第8–15行按 URL 是否为空分类并求和 | 接入库存、指纹、哈希、依赖、所有权、每卷空间与备份需求 |
| Worker 没有工作能力 | FlowPack.Worker/Program.cs 只有一行 Console.WriteLine | 建立 IPC、持久化状态机、操作日志、执行器与恢复器 |
| 数据及核心能力缺失 | 无 SQLite、下载、归档导出、部署、恢复、升级、卸载或迁移服务 | 按本文工作包逐项实现 |
| 主题只是临时改色 | 只切换五个颜色，校验未接到界面；多处参数硬编码 | 独立 DesignSystem 与完整 ThemeService |
| 工程和发布不完整 | 无 DesignSystem/Tests、依赖锁与 CI；Smoke 未进 solution；Release 缺项目映射；PNG未成为 EXE图标 | 修复配置、补正式测试与品牌/发布资源 |

WPF 本地属性值优先于样式触发器；模板需要显式使用控件属性。这与源码中的空白和间距问题相符，参见 [Microsoft 属性优先级说明](https://learn.microsoft.com/en-us/dotnet/desktop/wpf/properties/dependency-property-value-precedence)及 [TemplateBinding](https://learn.microsoft.com/en-us/dotnet/desktop/wpf/advanced/templatebinding-markup-extension)。

当前 Smoke 只检查窗口构造/标题、少量主题边界、大小求和、一个路径拒绝案例和一个正确 JSON。未检查窗口显示、导航、布局、实际安装或截图。本轮未重新执行应用测试；此前“构建通过”和“窗口句柄有效”不能作为这些功能的验收证据。

## 3. 两份设计的统一决定

| 项目 | 最终要求 |
|---|---|
| 主框架 | 顶部应用栏，品牌左、导航独立居中、环境状态与设置右；取消全局侧栏方案 |
| 导航 | 首页、资源包、工作流、任务；当前项蓝色文字和3 DIP下划线；二级页归属一致 |
| 首页 | 按01参考图的居中欢迎区域、导入/创建、环境卡片、当前任务构成；导入主按钮、创建次按钮；不另加统计、推荐、最近资源或额外工作流卡片 |
| 列表 | 纵向留白卡片行；不做原方案的卡片/列表切换；原要求的类型、大小、路径、引用等信息仍可通过行内元数据和详情查看 |
| 模型与节点 | 位于资源包下；提供可见的二级入口和返回路径，不新增顶层导航 |
| 创建向导 | 四步，水平步骤条；选择工作流 → 检查依赖 → 填写信息 → 导出；在线/离线选择并入第3步、完整性检查并入第4步 |
| 外观和设置 | 右上角设置入口，内有环境、资源库、下载、外观、诊断；外观按09参考图左编辑右预览 |
| 进度展示 | 下载实时进度仅首页当前任务与任务中心；其他页显示阶段结果及“查看任务” |
| 默认尺寸 | 窗口1280×800 DIP，最小960×640 DIP；参考画布1600×1000；工作区不足时降级，不把控件挤出可用区域 |
| 视觉参数 | 应用栏80 DIP、左右边距52 DIP、卡片圆角12 DIP、按钮/输入框默认6 DIP；Segoe UI与Microsoft YaHei UI回退；正文14 DIP |
| 主题 | 默认跟随系统；浅色遵循参考、深色保持相同层级；允许用户覆盖主题参数 |
| 窗口控制 | 保留正常可用的系统窗口控制，系统标题栏与应用内80 DIP栏分别计算 |
| Logo | 当前PNG为唯一源；保持F轮廓、像素风与黄色配色；不据此把界面主操作改成黄色 |

参考图中的日期、模型/节点版本、可用空间、速度、已节省空间和连接状态均是示例。产品只显示测量或记录所得数值；不能为贴图而填入假状态。缺少可靠统计口径时不显示“节省空间”。

## 4. 工程、进程与数据架构

### 4.1 技术栈与模块边界

| 模块 | 具体职责 |
|---|---|
| FlowPack.App | WPF View、页面ViewModel、路由、选择上下文、用户确认；CommunityToolkit.Mvvm的异步命令与可观察状态 |
| FlowPack.DesignSystem | 语义主题参数、ResourceDictionary、DynamicResource、统一控件模板、DrawingImage图标和组件预览页 |
| FlowPack.Core | 领域模型、依赖图、安装/升级/卸载规划、所有权规则、状态转换；不依赖WPF |
| FlowPack.Infrastructure | Microsoft.Data.Sqlite、HttpClient、SHA-256、ZIP64、文件操作、凭据、日志、IPC实现 |
| FlowPack.ComfyUI | Desktop适配器、工作流解析、Registry解析、路径配置、Python探测、本地HTTP/WebSocket |
| FlowPack.Worker | 组合服务、调度、单写者、实例锁、下载/部署/归档执行、进度持久化与恢复 |
| FlowPack.Tests | MSTest单元与集成、Windows进程/界面验收、故障注入；现有Smoke仅作为补充并接入解决方案 |

技术基线保持 C# + .NET 10、System.Text.Json、YamlDotNet、Microsoft.Extensions.DependencyInjection/Logging、System.Diagnostics.Process、Windows Credential Manager。不因当前没有依赖而放弃已选择的架构。

M0确认本机可用的 .NET 10 SDK后写 global.json；锁定 NuGet版本与 packages.lock.json，CI使用锁定还原。Debug/Release都必须实际构建全部必需项目。工程项目可叫FlowPack.App，最终主程序输出名固定为ComfyUI.FlowPack.exe。

### 4.2 运行边界

- App只负责交互与显示，ComfyUI和资源库的业务写操作由Worker执行。主题/应用偏好由专用配置服务管理。
- Worker按资源库唯一运行并持有独占锁；同一实际实例跨资源库也用规范化实例标识加进程间锁，避免两个库同时部署。
- 下载可并行，默认最多3个资源传输，可在下载设置调整；同一实例部署、修复、升级和卸载互斥。
- 命名管道限定当前Windows用户，启动握手校验协议版本与会话随机凭证。包内容不能定义任意命令或获得IPC权限。
- UI断开不代表任务取消。关闭有任务的窗口时提供“后台继续 / 请求安全暂停后退出”；不强杀Worker。重开后连接已有Worker并恢复订阅。
- FlowPack实例锁只能协调FlowPack进程；无法锁住其他管理器。每个写入阶段前复检目标文件和进程，发现外部变更立即停止并重新规划。
- 全局异常写入脱敏诊断；页面错误区域给出原因和可执行下一步，不能吞异常或把失败改成成功。

### 4.3 存储位置及数据库

用户配置使用用户级应用配置目录；允许显式绝对路径的数据目录覆盖用于测试。测试启动必须确认使用隔离路径，不能污染真实配置。

资源库逻辑布局：models按ComfyUI模型类别保存可用名称；node-archives按来源与精确版本保存归档；workflows、assets保存原件；packages保存清单及导入来源；staging保存未验证文件；cache保存可重建下载缓存；state保存数据库/任务日志；backups保存受管变更备份。所有目录布局写入版本化库元数据，不能通过硬编码推断旧库位置。

SQLite至少包含：

| 数据集合 | 必需记录 |
|---|---|
| Libraries、Instances | 库ID、实际路径、文件系统、实例绑定、配置来源、适配器、能力和指纹 |
| Packages、PackageVersions | 稳定包ID、版本、原清单、兼容/分发声明、完整性状态 |
| Resources、ResourceLocations | 哈希、大小、种类、来源、库内位置、外部只读位置和最后校验信息 |
| Workflows、Dependencies | 原件、入口、解析版本、依赖关系、依据、未解决项 |
| Installations、InstallationResources | 包版本/实例关联、引用、部署路径、所有权、原始/部署后哈希 |
| Tasks、TaskEvents、OperationJournal | 阶段、进度、错误、操作意图、前后状态、备份、重试、恢复游标 |
| VerificationReports、Drafts | 本地各级验证证据、指纹绑定、向导草稿及草稿修订 |

以数据库事务维护引用关系；文件系统与SQLite不伪装为同一个原子事务。采用“记录意图 → 写入/替换 → 验证实际结果 → 提交完成记录”，启动时核对未完成步骤。默认使用单写者和SQLite回滚日志；NTFS/exFAT分别验证锁、持久化和故障恢复，不能照搬仅在NTFS上验证的假设。

软件升级前备份数据库与schema版本；升级失败保留原库并进入恢复界面，禁止自动清空。数据有明确迁移器；发现比软件更新的schema时拒绝写入，说明需升级软件。

## 5. 核心接口与持久化契约

以下为实现基线，M1冻结格式1的schema、样例与错误码。后续改变影响已有清单时必须增加迁移或新格式版本，不能静默改义。

### 5.1 领域类型

- InstanceDescriptor：稳定实例ID、Desktop发行/版本、程序根/数据根/用户目录、解释器绝对路径、节点/模型/工作流/素材路径、本地服务、适配器及能力集合。探测时间与指纹内容分开，避免每次检查都因时间变化误报环境变化。
- EnvironmentFingerprint：ComfyUI/Python版本、解释器身份、已装Python包集、节点版本与修改摘要、相关配置内容哈希、目标路径与卷身份。执行复检按实际影响范围比较。
- PackageManifest：formatVersion、id、version、name、description、author/source、封面、入口工作流列表、兼容范围、resources、依赖关系、Python锁、分发/联网声明、创作者验证声明。软件版本与格式版本独立。
- ResourceEntry：稳定ID、资源类别、逻辑文件名/模型类别、精确版本或提交、大小、SHA-256、下载来源或包内相对路径、部署用途、分发状态及依赖ID。不允许由清单指定任意绝对部署路径或脚本。
- WorkflowAnalysis：UI/API格式、入口、已解析依赖、来源依据、缺失/冲突/动态未知项、可用验证能力和原始JSON；不能只返回“资源数组”丢失不确定性。
- InstallPlan：planId、清单摘要、实例/库ID、输入快照、动作依赖顺序、复用/新增/冲突、每卷峰值、备份/停机条件、可执行标志和阻断原因。用户确认绑定此planId与摘要。
- InstallationRecord：执行计划、实际资源与路径、节点/Python变更结果、引用、所有权、备份、当前完整性和验证状态。
- VerificationReport：各级独立状态、检查项、证据、实例指纹、时间、错误；创作者的报告不能自动成为接收端本地通过记录。
- ThemeDefinition：独立格式版本、名称、light/dark基底、语义颜色覆盖、字号、圆角、密度、动画。系统跟随偏好存于应用设置。

### 5.2 服务和行为约定

| 接口 | 输入与结果 | 副作用边界 |
|---|---|---|
| InspectInstance / DiscoverInstances | 候选路径/发现线索 → 描述、指纹、能力、未确认项 | 只读探测；不安装依赖、不改配置 |
| AnalyzeWorkflow | 原件+可选实例 → 依赖图/未知项/解析报告 | 保留原件，外部查询可取消 |
| ImportPackage | 文件/URL/目录 → 校验报告+包版本记录 | 校验与入库，不部署 |
| PlanInstall | 包版本+实例+库存快照 → 不可变计划 | 不修改目标环境 |
| ExecuteInstall | 已确认planId+摘要 → 持久化taskId | Worker复检后按计划执行 |
| BuildPackage | 草稿修订+交付方式+输出位置 → 导出任务/产物报告 | 只输出到确认位置；不覆盖同名文件而不提示 |
| VerifyInstallation | 安装ID+请求级别 → 分级报告/任务 | 实际试运行另有明确授权，不在静态检查中提交生成 |
| Plan/ExecuteRepair、Upgrade、Remove | 安装ID/新版本 → 预览计划 → 维护任务 | 复用所有权、指纹与事务规则 |
| Plan/ExecuteCleanup、Migration | 显式选择/目标库路径 → 预览 → 任务 | 不清理未知文件；迁移保留旧库 |
| Preview/Apply/Cancel/ResetTheme | 主题草稿 → 校验/预览/提交或恢复 | UI与用户偏好；不重启Worker |
| Import/Export/ReloadTheme | 版本化JSON/文件 → 校验与往返 | 拒绝任意XAML/脚本；坏文件不覆盖有效主题 |
| Query/Subscribe/ControlTask | taskId+游标/操作 → 快照、事件、确认结果 | 控制动作幂等，可安全重连 |

所有长操作支持CancellationToken并在页面提供忙碌状态；异步命令禁止重入，异常按校验错误、冲突、权限、网络、磁盘、取消和内部错误分类。用户提示和脱敏技术详情分开。

### 5.3 IPC与任务状态机

IPC消息包含protocolVersion、requestId、command、payload；响应包含成功结果或结构化错误。任务事件带taskId、sequence、阶段、字节数/总量（未知可为空）、时间与错误。重复requestId不能生成第二次部署；重连先读快照再补游标之后的事件。

安装主路径：

待确认 → 预检 → 下载 → 校验 → 等待实例停止 → 锁定与复检 → 部署 → 验证 → 完成。

分支状态：阻断、暂停请求中、已暂停、取消请求中、已取消、失败、恢复中、需人工处理。部署已完成但未能启动环境验证时记“部署完成，待环境验证”，不显示完整验证通过。参考图中的百分比不能用于驱动状态。

下载可立即暂停；文件提交/依赖安装等不能安全打断的步骤在边界暂停或取消，界面显示“正在等待安全停止”。失败重试创建关联尝试，先校验实际结果，只重做不完整步骤，不把操作机械重放。速度、剩余时间基于实际传输采样计算，无法估算时不显示假倒计时。

## 6. Desktop实例与资源库工作包

### 6.1 分版本适配与绑定

实现候选发现、手动选择、绑定确认、切换、复检和断开关联。候选来源为官方配置、安装记录与受控进程探测；禁止递归扫描整盘寻找第一个python.exe。

探测交叉核实配置记录、Python解释器身份、ComfyUI用户目录、服务地址、启动参数和实际资源位置；服务只响应HTTP不等于目录已绑定。界面分别显示“未绑定、待确认、已绑定但停止、服务可连接、版本未支持、路径离线、权限不足”。

官方存在不同Desktop实现，不能把一套旧路径用于全部版本。设计 DesktopLegacyAdapter 和 DesktopManagedInstallAdapter，通过现场可识别能力匹配；官方源码分支只提供适配依据，不能代表用户已安装版本。[旧实现说明](https://github.com/Comfy-Org/desktop/blob/main/README.md)、[另一官方实现说明](https://github.com/Comfy-Org/Comfy-Desktop)。

每个适配器必须明确：可读配置、Python探测方法、可写目标、持久化模型路径扩展点、节点安装位置、重启方式、版本匹配和拒绝条件。资料无法证明的行为列入M2真实环境测试，未通过不启用该写能力。

Manager形态按实际版本识别；不自动另外装一份旧版Manager，不把Manager管理的节点标为FlowPack所有。[官方Manager安装说明](https://docs.comfy.org/manager/install)

### 6.2 资源库创建、关联与索引

- 创建或打开本机NTFS/exFAT目录，检查读写权限、可用空间和独占锁；拒绝网络共享、多机写入和不支持文件系统。
- 拒绝位于FlowPack或Desktop程序目录中的库。目录边界比较应区分 C:\FlowPack 与 C:\FlowPackLibrary；解析已有链接/联接的最终位置，写入时再次核验，阻止绕过保护。
- 对已存在内容先预览；仅初始化专用受管目录，未知文件不移动、不删除。不因目录非空而自动覆盖索引。
- 原模型库支持只读索引和“复制到受管库”两种明确入口。外部模型默认不改动，复制时校验、记录来源并保留原件；存在稳定搜索路径时允许经确认复用。
- 长扫描由后台执行，支持取消、显示实际结果；目录断开标离线，重连用库ID验证，不能把另一个同盘符目录当原库。
- 模型以大小/哈希确认复用；仅同名不能证明一致。同名不同内容的部署必须阻断，允许用户先在副本中调整名称和工作流引用，再重新规划。

### 6.3 外部模型路径与实例配置

模型共享走外部搜索路径；节点部署到适配器确认的用户节点位置，Python依赖进入该实例确认的环境；工作流和素材部署副本。生成结果继续由ComfyUI管理，默认不打包。

修改前备份完整配置并校验原哈希；优先写专用受管配置段/文件，确保不改变他人配置语义并尽量保留注释。不能可靠保留时阻断并展示差异。不能覆盖Desktop程序资源目录，也不能反复写会被Desktop重新生成的配置文件。

外部节点搜索路径不等于依赖已安装，也不等于该路径是默认节点安装目录。[Windows官方说明](https://docs.comfy.org/installation/desktop/windows)；某些实现的模型配置由程序生成，须验证重启及升级后的持久性：[models.ts](https://github.com/Comfy-Org/Comfy-Desktop/blob/main/src/main/lib/models.ts)。

## 7. 包规范、工作流与依赖工作包

### 7.1 三种包输入与归档布局

线上清单支持从文件导入以及输入明确的HTTPS清单地址；离线支持.cpack和展开目录。格式1归档根目录固定manifest.json，资源使用payload下的相对路径，预览/创作者声明为可选附加内容。离线和展开目录共用同一校验器。

校验字段类型、格式版本、ID唯一性、引用完整性、大小边界、SHA-256格式、来源协议和包内路径。必需数据未齐可作为草稿导入但禁止执行；旧简化清单不静默变成完整包，提示补全缺失字段。

解包逐项限制实际展开量并对照声明；拒绝绝对路径、父目录逃逸、盘符/UNC/ADS、Windows保留名、大小写冲突以及链接/联接越界。目标均由适配器和资源用途计算。大模型以不压缩方式流式写ZIP64，不能整文件读入内存，超过4 GB必须实测。

输出先写同卷临时文件，完整关闭、校验后才发布为最终文件；异常保留可识别临时状态，不出现成功名称的半包。包内无本机凭据、绝对个人路径或带令牌的来源URL。

### 7.2 工作流解析

按官方 [v0.4](https://docs.comfy.org/specs/workflow_json_0.4) 和 [v1.0](https://docs.comfy.org/specs/workflow_json) 分版本解析UI工作流，保留原始JSON及未知字段。提取节点类型、模型引用、输入素材、连线和入口；保存解析依据与规则版本。扩展节点无法判定时保留原件并列人工补全，不用字符串猜测宣称全量解析成功。

UI保存格式与API执行格式分开保存。包可带apiWorkflow入口；仅在已实现并验证的映射规则下转换UI工作流。任意自定义节点的widgets_values不得通用猜测；缺API入口时提供从ComfyUI导出API文件并导入的入口。[官方API格式说明](https://docs.comfy.org/development/api-development/workflow-api-format)

### 7.3 依赖解析与人工补全

结合清单、工作流元数据、官方Registry、实例object_info、已装节点版本和内置解析规则，输出“已确定/冲突/缺失/动态未知”，显示每项依据。多个来源不一致时要求选择，不偷偷选第一个；Registry超时/离线使用带时间的缓存，过期或不确定状态明确展示。

补全界面允许选择本地模型/素材、指定节点精确来源与版本、添加Python锁定项、标记运行时下载/API和填写分发信息。用户选择不改原件；产生新草稿修订。无法确认的必需依赖阻止完整导出或安装，但允许保存草稿。

## 8. 下载、Python预检与安装工作包

### 8.1 下载与凭据

HttpClient流式下载到staging，支持HTTP Range续传、取消、超时、代理和可配置并发。记录ETag/Last-Modified及已验证片段状态；206范围必须匹配，服务器返回200或内容身份改变则重新下载；416核对远端大小与本地实际内容。重试使用退避，鉴权失败和永久404不盲目循环。

凭据放Windows Credential Manager，设置页面允许按来源配置/清除。导入清单只含来源标识；跨主机重定向不透传Authorization，带令牌URL不写入清单/日志/诊断。TLS校验保持开启。

通过字节数与SHA-256校验后才登记可用资源；已有缓存重新确认后可复用。哈希失败隔离失败文件、显示来源与重试动作，不进入正式库存。取消或清缓存不会删除在用资源。

### 8.2 Python依赖保护

使用已确认解释器的绝对路径，不使用PATH中的全局pip。安装前读取Python版本、架构、wheel兼容标签、已装包版本及依赖状态；预先存在问题单独记录，不伪装成本次安装造成或本次已修复。

仅对兼容wheel候选做解析预检，解析直接及传递依赖；已安装版本作为不可替换约束。如果求解要求升级、降级、卸载或覆盖任一既有版本，必须在目标环境任何写入前停止，并列出包名与版本冲突。

冻结wheel的名称、版本、文件、哈希和完整依赖图，执行前再次比较环境指纹；本地安装仅使用冻结文件，不在执行阶段重新从网络解析版本。安装后依赖检查加节点加载验证，不能把pip成功等同于生成可用。

新增包名不等于不会覆盖文件。安装前将冻结wheel的实际安装路径与现有文件及已装distribution的RECORD对照，检查site-packages、Scripts和共享命名空间的文件碰撞；将覆盖现有文件或不能证明所有权的候选阻断。pip中断后标记“Python环境待修复”，复验文件与依赖状态后才可继续，不能仅凭dist-info存在判断完整。

pip的dry-run报告只是预检输入，不是锁文件；--no-deps不能保证已有版本不被替换；源码元数据分析可能执行构建后端，因此源码包与自定义安装脚本必须转入专门适配，不自动运行。见 [pip install](https://pip.pypa.io/en/stable/cli/pip_install/)、[安装报告](https://pip.pypa.io/en/stable/reference/installation-report/)、[构建机制](https://pip.pypa.io/en/stable/reference/build-system/)。

### 8.3 安装规划和执行

1. 读取包版本、库存、引用、节点修改摘要及环境指纹，计算复用、新增、下载、部署和阻断。
2. 每个磁盘分别计算下载暂存、解包、部署副本、备份与提交期间并存文件的峰值；不能仅把包大小相加。空间估计依据和无法确定项随报告展示。
3. 预览按目标实例展示动作与冲突，用户确认生成只读计划快照。校验失败、不支持、缺依赖或空间不足时“开始安装”禁用并解释原因。
4. 下载并校验可在ComfyUI运行期间进行；写目标前等待其停止。队列忙时提示等待，不能清空队列、强杀进程或自动停止他人任务。
5. Worker取得实例锁，复检计划摘要、源文件、目标、配置、空间和解释器；变化则使旧计划失效，回到预览。
6. 预记录可恢复操作，暂存节点和工作流，安装已批准的新增wheels，更新受管配置并提交文件；每步有步骤ID、预期前后哈希、备份/恢复策略。不存在全部跨文件原子性的承诺。
7. 检查已提交文件和引用，保存安装结果；提示用户启动/重启目标实例，继续环境和静态验证。请求主动试运行后才进入第四级。
8. 任一步失败停在可解释状态，保留备份和日志，恢复器核对实际文件；不得通过进度达到100%登记安装成功。

实例锁仅协调FlowPack自身，不代表Desktop或Manager遵守该锁。部署期间还要确认目标实例停止且没有已识别的安装/更新操作，每个写入阶段检查相关进程及指纹；无法确认外部写入状态时提示用户停止相关操作后继续。

本地修改节点或外部工具所属节点不得覆盖。受管节点版本替换也须明确列入升级计划并检查所有引用包兼容性。Python新增包在失败恢复/卸载后默认保留并记录，不盲目卸载；只恢复有确切所有权与备份依据的文件、配置，第三方代码副作用不保证完全回滚。


## 9. 打包向导、分发与离线交付工作包

### 9.1 四步完整行为

| 步骤 | 输入/动作 | 输出与通过条件 |
|---|---|---|
| 1 选择工作流 | 从库选择一份或多份工作流，或导入JSON；指定入口与可选API执行文件 | 保留原件、格式验证、形成草稿；无有效工作流不能继续 |
| 2 检查依赖 | 自动分析、核对实例、选择资源、补未知依赖、查看冲突 | 依赖图与未解决项可见；允许保存未完成草稿，不允许伪称完整 |
| 3 填写信息 | 名称/版本/说明/封面/来源、兼容条件、分发声明、在线或离线、目标平台与输出位置 | 字段完整、所有资源的交付方式明确；不能把凭据写入元数据 |
| 4 导出 | 最终完整性预检、收集模型/节点/wheels/素材、计算哈希、写ZIP64或清单、重读验证 | 原子提交最终产物，显示路径/大小/完整性/联网需求；失败保留可恢复任务而不报告成功 |

前进、后退不丢编辑；自动保存草稿修订并提供显式保存；重新进入可继续。重复点击导出只产生一个任务。取消只影响当前任务，保留草稿。资源缺失时定位到具体项并回到第二步。

### 9.2 在线与离线的准确声明

- 在线清单必须让接收端知道精确资源身份、可获取来源、鉴权需求与兼容条件；失效链接可重新指定来源，但新内容哈希不同必须生成新修订。
- 离线包收集目标Python/平台所需的完整可分发wheel闭包，不把当前开发机“已经装过”当作接收端不需要。用隔离解析/目标标签构建交付锁；真实安装预检仍按接收端现状保护既有版本。
- “本地文件齐全”“安装阶段可离线”“试运行仍需联网”分别展示。含外部API、运行时下载、人工获取资源的包不得显示为完全离线可运行。
- 分发状态为“已确认允许附带 / 仅获取信息 / 未确认”；未确认默认不包含二进制。记录确认依据，不自动作法律保证。若必需文件因此缺失，只能导出如实标注的不完整包/在线获取清单。
- 生成结果默认不包含，环境、解释器、驱动和令牌不打包。输入素材需用户选定；隐私路径和用户凭据从导出物中排除。
- 离线合格包必须在满足前置条件的另一目标实例断网安装通过；仅在本机可解压不构成离线验收。

## 10. 四级验证、工作流试运行与维护工作包

### 10.1 四级报告

| 级别 | 必须有的证据 | 不允许替代它的证据 |
|---|---|---|
| L1 文件完整 | 所有必需文件存在，大小和哈希匹配 | 下载请求成功、ZIP生成或进度100% |
| L2 环境就绪 | 目标实例匹配，节点实际加载，目标模型可见，依赖检查结果 | 找到python.exe、pip返回0、TCP端口开放 |
| L3 静态检查 | 工作流结构、节点类型、连接和可解析输入引用通过；未知项明确记录 | 把普通JSON提交生成或只检查JSON语法 |
| L4 主动试运行 | 用户确认执行、记录本任务prompt_id、成功历史与实际输出，满足约定输出条件 | 排队成功、WebSocket已连接、他人任务完成 |

各级可以分别为未执行、通过、失败、阻断；L1通过后L2待启动不能显示全通过。L4输出“符合预期”需要可定义的文件/尺寸/数量等条件；审美等主观条件由用户确认，不自动判定。

L3任一必需结构/节点/输入检查失败则失败；必需项动态未知或未完成解析则阻断，不能一边显示未知一边宣称通过。只有可选且不影响当前入口的未知项可作为警告；一次试运行成功不清除其他入口未检查事项。

读取本地system_stats、object_info、模型目录、queue、history和WebSocket信息时绑定已确认实例；服务端信息不能独自证明解释器路径。普通静态检查不调用POST /prompt，因为该端点校验成功会进入执行队列。[官方路由](https://docs.comfy.org/development/comfyui-server/comms_routes)、[server.py](https://github.com/Comfy-Org/ComfyUI/blob/master/server.py)。

试运行只跟踪自己的prompt_id。取消未开始任务仅移除自身队列项；运行中的任务没有安全的定向取消能力时提示等待，不用全局interrupt中断其他任务。网络断开后通过history恢复观察，不能擅自重复提交生成。FlowPack仅在用户明确确认试运行后主动提交生成；环境验证需要启动实例时先提示，ComfyUI启动及第三方节点加载本身可能占用GPU/内存，不能保证无副作用；含外部API时另提示实际联网/账户需求。

Desktop、节点、依赖或路径配置发生关联变化后，对应验证报告标记“需复检”，保留历史证据。

### 10.2 修复、包升级、卸载和清理

- 修复先生成缺失/损坏/被修改清单；只修复有所有权且用户确认的项，重新下载校验后受管替换。用户修改过的文件默认保留，提供差异和手动处理。
- 包升级导入新版本或用户触发检查作者提供的更新清单；比较包版本与依赖差异，生成复用/新增/替换/保留预览。来源未提供更新信息时显示“无法检查更新”，不伪造“最新”。
- 原包版本、备份和安装关联保留到新版本验证及提交完成。共享节点升级检查所有引用包；不兼容时阻断，不偷偷破坏另一个包。
- 卸载先显示释放哪些引用、会删哪些受管文件、会保留哪些内容。只删除FlowPack部署、无其他引用且未被修改的节点/工作流及受管素材副本；原件保留，模型与Python默认保留。
- 未知、外部管理或用户修改的文件不因“名字相同”被删除。卸载只移除本软件写入且不再需要的路径配置条目。
- 清理在用户触发扫描后给出缓存、无引用模型、临时文件的实际清单与空间；用户明确选择并确认后执行。库离线、引用状态不完整或任务在用时不能清理。
- 破坏性确认列明目标、保留项及是否有可恢复备份；没有撤销能力必须明确说明。维护命令同样走Worker、日志、指纹和实例互斥。

### 10.3 崩溃恢复和资源库迁移

从journal识别“意图已写但文件未完成”“文件已完成但数据库未提交”“配置被外部改动”等状态，按实际哈希恢复或转人工。幂等步骤不得重复创建引用、重复安装或删除恢复过程中出现的新文件。

迁移过程：预检目标文件系统/路径/空间 → 暂停相关写任务并锁库 → 复制资源与一致性数据库快照 → 全量校验 → 停止相关实例并预览路径变更 → 备份并更新每实例受管路径 → 提交新库关联 → 保留旧库。不能跨盘使用“移动”冒充已校验迁移。

迁移预检拒绝源等于目标、目标在源内、源在目标内及链接/联接解析后成立的同等情况。目标已有另一资源库ID时停止，不能合并覆盖；恢复同一次迁移须核对迁移ID与清单。

若任一实例无法安全更新路径，不把迁移标为成功；恢复可恢复的配置和关联，保留两份数据及错误报告。断盘、权限/空间错误时原库仍有清晰的有效状态。旧库须标记迁移状态，避免两份同ID库被误认为可同时写；清除旧库由用户后续明确决定。

## 11. 页面、组件、主题与Logo工作包

### 11.1 页面及全部可用入口

| 页面/参考 | 页面必须可以完成的操作与数据来源 |
|---|---|
| 首页 / 01-home.png | 显示实际绑定/连接/库状态；导入资源包、创建资源包；无任务显示空态，有任务读取Worker快照并可跳任务中心 |
| 资源包 / 02-package-list.png | 三种包输入、名称搜索、全部/已安装/有更新状态筛选、版本与安装实例；进入选中包详情；二级入口模型库和节点管理 |
| 包详情/预览 / 03-package-install-preview.png | 真正选中的包名、说明、兼容、体积、入口、依赖、分发/联网要求、四级验证、每卷空间与冲突；查看完整清单；唯一主操作开始安装 |
| 工作流 / 04-workflow-list.png | 文件导入、搜索、原件/预览信息、更新时间、节点和模型依赖摘要；查看依赖、补全和进入创建包 |
| 模型库 / 05-model-library.png | 导入本地模型、搜索、类别筛选；哈希就绪/缺失/离线状态；详情可见路径、大小、来源、引用和复制完整路径 |
| 节点管理 / 06-node-management.png | 按实例检查、搜索与状态筛选；版本、来源、加载结果、所有权与兼容异常；查看原因、修复/升级计划入口 |
| 任务中心 / 07-task-center.png | 进行中/历史；真实阶段、进度、速度、剩余量、时间；暂停/继续/取消/重试、错误详情和脱敏日志 |
| 创建资源包 / 08-package-wizard.png | 第9节全部四步、草稿恢复、依赖补全、交付方式、最终报告；按参考保持水平步骤条 |
| 外观 / 09-appearance-settings.png | 全部主题参数、实时预览、应用/取消/恢复、导入/导出/重载；右侧预览与全局资源一致 |
| 设置补充 | 环境候选/绑定/切换、库位置/关联/清理/迁移、代理/来源凭据/并发、诊断导出；使用相同视觉语言 |

资源包详情、模型与节点页高亮“资源包”；创建向导按参考图高亮“工作流”；外观为独立设置上下文，不错误高亮主导航。来自首页或列表的入口共享同一路由，不复制业务。只有危险操作使用确认弹窗，一般错误、离线与进度使用页面状态。

导航通过ContentControl绑定当前页面ViewModel和DataTemplate；每页有独立View与可维护的XAML，禁止把九页全部压进一个长行文件再依赖互相隐藏。缓存各页选择/筛选状态，订阅生命周期正确解除。

### 11.2 组件与响应式验收

共享组件至少覆盖：主次按钮、图标按钮、搜索框、状态筛选、列表卡片、状态标签、页面标题、空/加载/错误区域、进度、向导步骤、确认弹窗、详情键值、长路径复制。

成功/警告/失败同时使用图标、文字和颜色，紧凑标签不代正文；浅色主按钮钴蓝实色，次按钮白底蓝描边，其他主题使用对应语义资源。破坏操作不与页面主操作并列；常规页面标题包含一行说明、主操作在标题行右侧，首页和向导采用相应参考图位置。下载进度条细、单色、无装饰动画。

按钮模板必须消费Padding、对齐、边框等属性，并覆盖鼠标悬停、按下、禁用和键盘焦点；图标按钮必须有可访问名称。正式程序不保留点击无反应的按钮；能力不满足时禁用并说明原因。

主导航叠放在全宽根Grid中央，不参与品牌和状态宽度分配；品牌/状态限制宽度并可展开详情，不能挤压居中组。工程验收以导航中心与窗口内容中心相差不超过1 DIP为目标。小于960 DIP的工作区降级保留品牌、状态和当前页，其他导航放菜单。

列表用可回收虚拟化容器、分页/增量读取和按需封面；避免外层无限ScrollViewer使虚拟化失效。字号到20 DIP或高DPI时允许换行、纵向滚动/独立详情页，主操作必须仍可达。

### 11.3 完整主题生命周期

- 主题模式：浅色、深色、跟随系统；系统变化仅在跟随模式更新，高对比优先系统颜色。
- 参数：强调色及主要语义颜色、正文12–20 DIP、标题等按比例生成、圆角0–16 DIP、舒适/紧凑、动画开关。默认轻量过渡约150毫秒，遵循系统减少动画；进度条不加装饰动画。
- 白名单JSON只包含版本、名称、基底和可覆盖参数，禁止脚本、远程资源、任意XAML。颜色、类型、枚举、范围、必需字段和输入大小均校验，空值不能导致崩溃。
- 主题导入、导出和重新加载使用.flowpack-theme.json；文件选择器和默认导出名称采用该扩展名。
- 检查正文在窗口、卡片、输入框等实际背景上的对比度；不足4.5:1明确指出组合并阻止应用。强调按钮文字也按其实际底色校验，不能只测试一对颜色。
- 编辑/导入先形成预览快照；应用保存有效主题和偏好，取消/退出预览恢复前值；恢复默认先预览，用户应用后落盘。
- 损坏导入保留当前有效主题；启动加载失败回退内置并提示；外部编辑仅通过“重新加载主题”触发，不自动监听文件。
- 主题JSON导出再导入，全部受支持参数语义一致；内置主题随程序、用户主题在配置目录，与.cpack分离。
- 切换不能重建任务服务、重启Worker、取消下载或丢失当前选择/筛选/向导输入。
- 工程用语义DynamicResource和集中样式；页面不得写死颜色、字体大小、圆角、阴影或复制模板。固定图标使用StaticResource。

### 11.4 Logo与品牌交付

读取当前PNG的真实透明通道与边缘质量，保留原文件；按原轮廓导出可复现的多尺寸ICO（16/24/32/48/64/128/256）并记录生成方法，不能用字体F或重绘图替换品牌。

如原图有实底，先保留其原始背景形态，不自动把背景抠掉改造Logo；检查浅深主题下是否需要中性承载底。输出到应用资源后同时设置EXE ApplicationIcon、Window.Icon、任务栏、快捷方式和安装器图标。小尺寸、不同缩放及真实安装后检查，不能以窗口内有PNG代替系统图标完成。

## 12. 工程质量、诊断与正式交付工作包

- 用MSTest建立Core、Infrastructure、ComfyUI与Worker的单元/集成测试；所有测试和Smoke进入solution，CI明确运行实际测试并核对用例数量。
- 增加Windows UI自动化项目：启动隔离配置的真实EXE，检查首屏可见、导航点击、输入、筛选、任务和主题；渲染树/自动化树+实际截图联合验收。
- 诊断导出包括应用/适配器版本、任务关联ID、失败阶段、配置存在性和脱敏日志；预览导出范围，不包含凭据、工作流私人提示词、完整原文件或无关个人路径。诊断包由用户选择导出，不自动上传。
- 日志结构化，Worker写业务事件；UI进度默认合并到每秒最多5次、日志批量追加。数据库写入使用阶段提交及节流快照，不随每个网络chunk事务写库。
- 发布为win-x64自包含目录，主程序ComfyUI.FlowPack.exe、Worker及全部运行时同包；不裁剪、不Native AOT、不依赖开发机绝对路径。
- Inno Setup仅管理软件文件，默认按用户安装，不为正常使用要求管理员权限；模型库和用户配置不在卸载删除清单内。
- 标准文件关联仅针对.cpack并提供明确选择；.cpack.json通过应用内导入、拖放或用户显式选择打开方式进入，不接管所有.json的默认程序。命令行只接收文件路径，不执行包中命令。现有应用实例可接收导入请求，不能重复启动多个写入Worker。
- GitHub Actions在用户指定的LightyearXizIl/ComfyUI-FlowPack仓库使用Windows Runner执行锁定还原、Debug/Release构建、测试、自包含发布与安装器生成；分支未连远程时先交付可本地运行的同等脚本，远程结果明确待验证。
- 安装/升级时检测FlowPack后台任务，等待或安全暂停后替换程序，保留数据库备份与用户主题；不在Worker仍写库时强制升级。
- 版本说明、产物SHA-256、支持环境表、使用手册、已知限制、测试报告与安装器一起交付。只有存在证书且实际签名验证通过才宣称已签名；缺证书如实记录。
- 公开上传、打标签、发布release和签名凭据配置属于实际发布动作，后续按用户授权执行；使用已指定的现有仓库。本规划包含相关构建与验证准备，不冒充已发布。

## 13. 分阶段执行顺序、依赖与阶段出口

所有阶段均为待实施/待验收。阶段出口是继续集成的条件，不是可删减其余功能的范围切割；项目最终完成须通过M0–M10及全部R/T条目。

| 阶段 | 工作包与主要输出 | 前置依赖 | 必须通过的出口 |
|---|---|---|---|
| M0 当前阻断修复与工程校正 | 拆页面宿主、修模板、去除默认假状态；补solution映射、SDK/依赖锁、Tests和隔离配置 | 当前源码/本文 | 首页和全部导航真实可见；无假连接；Debug/Release实际产物及首屏回归通过 |
| M1 契约/设计系统/持久化基础 | 冻结清单/主题/IPC格式1、schema/样例；七模块、SQLite迁移、DI日志、基础主题与组件预览 | M0 | 三个视觉样板首页/资源包/任务通过；空数据、退出重开、坏配置和迁移失败行为通过 |
| M2 实例适配与资源库 | 候选发现/手选确认、指纹、版本能力、库创建/关联、索引、路径保护 | M1 | 至少一套真实官方Desktop可正确绑定，两个隔离实例互不混淆，NTFS/exFAT和路径保护通过 |
| M3 资源包/工作流/依赖 | 三种包输入、两种UI工作流格式、API入口、Registry/本地分析、人工补全、列表搜索筛选 | M1、M2 | 导入真实文件保存后重开仍在；未知字段保留；依赖不确定性和格式错误可处理 |
| M4 下载与Worker任务 | IPC完整实现、单写者/实例锁、下载/缓存/哈希/凭据、状态持久化、初始journal与重连恢复 | M1、M2 | 本地测试HTTP故障/鉴权/续传、UI重连、Worker中断恢复及并发互斥通过 |
| M5 安装规划与真实部署 | 真实库存决策、每卷空间、Python预检、停机等待、备份/日志/安全提交、L1–L3验证 | M2、M3、M4 | 一份含模型/节点/工作流/素材/新增wheel的测试包完成部署；所有保护性阻断及故障恢复通过 |
| M6 创建/分享/完整离线/试运行 | 四步草稿、wheel收集、在线清单、ZIP64、联网/分发声明、主动L4 | M3、M4、M5 | 导出物在另一合格实例重导入；>4 GB包实测；合格离线包断网安装；本地试运行有自身输出证据 |
| M7 维护全流程 | 修复、包升级、卸载、清理、迁移、数据库升级/恢复与验证失效 | M4、M5、M6 | 两包共享资源不受破坏；修改文件保留；迁移中断旧库可恢复；升级重装关联保留 |
| M8 全页面/主题/品牌完成 | 九页真实状态、设置五分组、主题全部参数及往返/取消/回退、DPI/键盘、ICO各入口 | M1，随M2–M7接入 | 所有交互无空壳；九页截图/DPI矩阵、运行中换肤、Logo小尺寸验证通过 |
| M9 故障/性能/兼容综合验收 | 全需求追踪、故障注入、真实Desktop矩阵、大列表/大文件、长路径、日志脱敏 | M0–M8 | 本文第14节及追踪表全部必需用例通过；阻断缺陷为零；每项保留证据 |
| M10 安装器与交付 | 自包含产物、Inno Setup、Windows CI、本地使用/恢复说明、版本/校验报告 | M9 | 无额外.NET干净机安装启动，Worker可用，升级/卸载/重装数据保留；交付产物与测试版本一致 |

### 13.1 当前执行状态（2026-09-11）

- M0 本机工程出口已通过：单一页面宿主和九个独立 View 已落地；按钮模板、顶栏几何居中、主导航上下文及资源包二级入口已修复；正式启动为空资源、空任务和“尚未检查环境”，不再加载模拟连接与进度。
- `.NET SDK 10.0.112`、中央 NuGet 版本和 `packages.lock.json` 已锁定；Solution 已补齐 Debug/Release 与 Tests/Smoke 映射。本机 Debug/Release 各 12 项测试通过，Release 实际 EXE 首屏截图及 UI Automation 主导航/二级入口记录见 `artifacts/acceptance/M0/`。
- 本记录只关闭 M0 的本机工程出口，不关闭 T01、T02、T38 的完整矩阵。键盘全路径、150%/200% DPI 截图、带真实导入数据的详情切换、真实 Desktop/Python/Worker、安装与打包仍待后续阶段验证；M1–M10 保持待实施。

M0实现顺序固定为：有效页面宿主 → 模板Padding/可访问状态 → 导航上下文与二级入口 → 真实未检查/空任务状态 → UI回归 → 工程/Release校正。不能只移除Collapsed后继续用模拟连接宣布修复完成。

可并行的开发：M1设计系统与数据层；M3解析与M4下载；M8视觉/主题可随业务推进；测试夹具从M1开始持续开发。共享schema、IPC和数据库由同一契约负责人合并，Agent/工程师不得各自发明不兼容字段。Desktop与Python写入适配必须由负责人审阅后接主线。

不提供未经人员/机器/网络条件核实的工期数字。进度按工作包和验收证据计，禁止用已写页面数或代码行数折算“功能完成百分比”。

## 14. 验收方法、数据夹具与证据

详细R01–R38需求和T01–T42用例见 [REQUIREMENTS_ACCEPTANCE.md](REQUIREMENTS_ACCEPTANCE.md)。

### 14.1 测试环境与数据

- 所有自动化默认使用显式绝对路径的临时配置与资源库。真实Desktop写入测试使用专门建立的测试实例，先记录其指纹和外部环境基线。
- 至少两份包共享同一模型与节点；另有同名异内容、节点本地修改、外部归属和Python版本冲突样例。
- 工作流夹具包含v0.4、v1.0、扩展未知字段、有效API入口、无法转换节点与动态依赖。
- HTTP测试服务可返回正确206、忽略Range的200、416、401/403、404、5xx、中途断流、ETag变化、错误哈希和重定向。
- 真实>4 GB不可压缩测试资源使用流式生成并有足够磁盘；在NTFS与exFAT分别验证。不能只把声明sizeBytes改大。
- 每个变更边界注入失败：下载、校验、解包、配置备份、文件提交前后、pip步骤、数据库提交、重连与迁移。
- 清理与迁移测试不以用户现有模型库为实验对象；断网试验在隔离环境禁用目标网络访问，不更改用户全局网络/安全设置。

### 14.2 UI与性能门槛

以下为本项目拟定的工程验收目标，不是已测性能或外部标准：

- 1600×1000、1280×800、960×640逻辑内容尺寸及100%/150%/200%缩放；实际窗口含系统框架时标注客户端尺寸，不混用像素与DIP。
- 所有九页在主要尺寸/明暗主题下留截图；全部键盘路径、20 DIP字号、高对比、长中文/路径和窄工作区可达；导航中心误差≤1 DIP且不重叠。
- 以10,000条索引记录测试搜索与虚拟化，已加载页面的本地交互95分位反馈目标≤200 ms；网络/扫描等待立即显示忙碌状态，重操作不占UI线程。
- 下载、哈希和ZIP64不把完整大模型加载到内存；测试记录峰值工作集、持续传输与界面响应，内存不能随单个文件大小线性增长。
- 进度未知时显示阶段/字节数，不编造总量；暂停/取消确认与实际文件状态一致；换主题不新增任务、不丢选择。

性能结果记录机器、磁盘、版本、数据集和测量方式；未达到目标需优化或明确说明原因，不能改小数据集伪装通过。

### 14.3 证据格式与最终通过标准

按阶段保存证据到artifacts/acceptance/<阶段>/<运行标识>/，包括：构建版本/源码标识、SDK/依赖锁、目标实例指纹、测试TRX、UI截图与操作记录、脱敏Worker日志、关键前后文件哈希、归档/安装器校验值和环境矩阵。

只允许“待实施、实现中、待验证、已通过、有阻断”状态。已有少量辅助代码不自动算整条需求已通过。标已通过必须有证据相对路径、执行日期和对应构建；CI与本地、模拟夹具与真实Desktop、静态检查与实际生成分别记录。

正式完成须同时满足：R01–R38全部满足；T01–T42必需场景通过；没有空白页/无响应入口/假状态；真实安装、离线导出再安装、试运行、维护保留和安装器验证全部完成。仅构建或窗口启动成功不满足任何业务闭环出口。

## 15. 已知未知、默认选择与后续核验

本轮没有检查用户安装的Desktop、真实Python、GPU或目标模型库，因此以下是执行阶段必须消除的未知，不能编造配置值：

| 未知 | 核验方式与阶段 | 未通过时行为 |
|---|---|---|
| 实际Desktop版本/发行结构/实例配置 | M2只读探测，记录版本与配置来源，用户确认绑定 | 允许报告与打包，禁用自动写入 |
| 可持久的模型/节点/工作流配置扩展点 | M2在测试实例中重启/版本变化检查 | 不改程序目录或自动生成文件；该能力保持阻断 |
| 解释器及wheel/GPU兼容 | M2/M5核对解释器/包集/标签，M6真实节点加载与输出 | 显示不支持项，不能用“pip成功”放行 |
| 他人管理工具同时修改的可能 | M4/M5故障与竞态测试，写前重新核验 | 使计划失效并暂停 |
| 任意节点的源码/脚本/运行时下载 | M3显式识别，支持规则及专门适配测试 | 标未知/联网需求，允许草稿，不声明完整离线 |
| 用户可用测试实例、磁盘与分发样本 | M2/M6选定隔离位置与可用测试资源 | 相关验收待验证，其他不依赖项可继续 |
| 已指定仓库的写入/CI权限、签名证书 | M0关联核对，M10发布配置核对 | 提供本地构建/未签名事实，不声称已远程发布或签名 |

默认选择已经确定：WPF/.NET 10、Windows x64、当前Logo、顶部导航、四步向导、跟随系统、14 DIP字号、12 DIP卡片、舒适密度、轻量动画、库单写者、下载并行3个、同实例串行、模型/Python默认保留。下载并发和动画是可配置默认值，不是能力上限。

## 16. 官方资料与事实边界

本轮已查阅官方文档及源码，链接均用于相应技术边界；没有把在线main分支当作本机部署版本。实施M2需记录真实适配版本和对应源码/文档快照。

- WPF空白与模板属性问题：第2节的Microsoft官方链接。
- Desktop实例结构、路径和Manager：第6节的Comfy官方文档及两个官方仓库。
- 工作流v0.4/v1.0/API格式：第7节官方schema与说明。
- Python预检、wheel限制和安装报告：第8节pip官方文档；执行后 [pip check](https://pip.pypa.io/en/stable/cli/pip_check/) 只作为依赖满足检查，不代表二进制/GPU运行验证。
- 本地队列/执行边界：第10节官方路由与服务端源码。
- 原始需求中的技术选择、尺寸、字号和对比度要求为本项目要求；本文新增并发、节流与性能数字为明确的工程默认/目标，均非已经完成的测量结果。

本规划交付后，应用当前缺陷仍存在；实际修复与功能完成状态以随后代码及上述验收证据更新。
