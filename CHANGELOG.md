# 更新记录

## 0.1.0 - 2026-09-11（本地候选，已构建，未发布）

- A–F 资源识别、依赖解析、导出、staging 下载和禁用的安装服务已独立提交封存。
- 壳层收敛为首页、资源库、打包、安装、任务、设置六个页面；资源库与安装页以 Tab 聚合旧视图，并保留既有选择与筛选状态。
- 增加简体中文/英文运行时切换及本地语言偏好；增加 GitHub Release 元数据检查，仅接受 HTTPS 安装器与同名 SHA-256 清单条目。
- 普通原生 ZIP 先解包到私有 staging，拒绝越界路径、盘符/ADS、链接、保留名、重复路径和超量展开；大模型导出使用不压缩 ZIP 条目。
- 资源库 schema 升级为 v6，迁移前备份，并预置资源、引用、安装、缓存、任务尝试、journal、备份和验证报告表。
- 增加保守 Desktop 适配器与冻结安装计划；真实 Desktop 写入、L1–L4、恢复、升级/卸载、断点续传和隔离实机验证仍未完成，因此安装保持禁用。
- 安装器新增语言选择、安装目录页和语言保留配置；仓库内中文语言覆盖仍需在正式发布前替换为固定的完整 MIT 上游翻译文件。

## 0.0.2 - 2026-09-11

- 设置页增加作者 LightyearXizIl、GitHub 仓库打开/复制入口和脱敏诊断摘要导出。
- 增加资源库 SQLite 持久化、主题偏好往返、Worker 本地管道协议与经 SHA-256 校验的 staging 下载。
- 支持格式 1 清单、`.cpack`/展开目录导入、原始工作流保留、草稿交换及不完整工作流包导出。
- 资源包、工作流、模型和节点列表支持本地筛选；任务中心只显示 Worker 持久化的真实任务记录。
- 安装按钮继续保持禁用；真实 ComfyUI Desktop 绑定、部署、恢复和安装生命周期尚未完成实机验证。

## 0.0.1 - 2026-09-11

- 建立 .NET 10 WPF 多项目解决方案。
- 修复页面全空、按钮 Padding 丢失和顶部导航偏移问题。
- 将九个页面拆为独立 View，并使用单一页面宿主切换。
- 移除正式启动时的模拟环境、模拟资源包和模拟下载任务。
- 增加 Debug/Release 测试、SDK/NuGet 锁定、发布脚本和 Inno Setup 安装器。
- 增加当前 Logo 的窗口、EXE、快捷方式和安装器图标接入。
